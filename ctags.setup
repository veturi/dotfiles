# Install CTAGS for VIM tag support from Brew
brew install --HEAD universal-ctags/universal-ctags/universal-ctags

# Alias it as macOS already ships with some tuned down version
# alias ctags="`brew --prefix`/bin/ctags"

